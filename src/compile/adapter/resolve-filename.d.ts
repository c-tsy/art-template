export = resolveFilename;
/**
 * 获取模板的绝对路径
 * @param   {string} filename
 * @param   {Object} options
 * @return  {string}
 */
declare function resolveFilename(filename: string, options: Object): string;
